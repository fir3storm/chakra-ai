# build output
build/
bin/
*.o
*.a
*.so
*.exe
compile_commands.json

# Model weights and derived data. Hundreds of gigabytes; never commit these.
# The patterns are broad on purpose; test fixtures are re-included at the bottom.
*.safetensors
trunk.bin
trunk.json
k3model/
k3trunk/
*.gguf

# Run artifacts. Anchored to the repository root so that a stray `k3_run.json` in the
# working directory is ignored while an identically-named fixture is not.
/*.log
/k3_run.json
/expert_trace.bin
/expert_hist.json
/st_index.json
/st_values.json
/out/
/results/

# credentials
hf_token*
*.pem
*_key
*_key.pub

# python
__pycache__/
*.py[cod]
.venv/
venv/
.ruff_cache/
.pytest_cache/
.kimipy_sessions/
.chakra_benchmark.json
.chakra_sessions/
chakra_output/
models/

# editors / os / misc
.vscode/
.idea/
.DS_Store
.commandcode/
*.swp
*~ 
